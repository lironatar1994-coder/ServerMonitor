# Used by `npm run dev:live` (vite --mode remote).
# Points the dev server's API proxy at the production monitor backend so the
# local UI renders real server data. No secrets here — just the public host.
VITE_API_TARGET=https://monitor.vee-app.co.il
